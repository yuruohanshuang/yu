HTTP 请求与响应结构

# HTTP 请求组成

| 组成部分 | 作用 | 示例 |
| --- | --- | --- |
| 请求行 | 说明方法、路径和协议版本 | `POST /posts HTTP/1.1` |
| 请求头 | 描述客户端、数据格式、认证信息 | `Content-Type: application/json` |
| 空行 | 分隔请求头和请求体 | 空白行 |
| 请求体 | 提交给服务端的数据 | JSON、表单、文件 |

# HTTP 响应组成

| 组成部分 | 作用 | 示例 |
| --- | --- | --- |
| 状态行 | 说明协议版本、状态码、状态描述 | `HTTP/1.1 200 OK` |
| 响应头 | 描述服务端和返回数据格式 | `Content-Type: application/json` |
| 空行 | 分隔响应头和响应体 | 空白行 |
| 响应体 | 服务端返回的业务数据 | JSON 数据 |

# 浏览器 Network 面板常见区域

| 面板区域 | 重点查看内容 | 排查价值 |
| --- | --- | --- |
| Headers | URL、Method、Status Code、Request Headers | 判断请求发到哪里、怎么发 |
| Payload | Query 参数或请求体 | 判断提交的数据是否正确 |
| Response | 响应体内容 | 判断服务端返回了什么 |
| Preview | 格式化后的响应 | 快速浏览 JSON 结构 |

# HTTP 请求方法
POST /posts HTTP/1.1
Host: jsonplaceholder.typicode.com
Content-Type: application/json
Accept: application/json

{"title": "demo", "body": "hello", "userId": 1}

# HTTP 响应示例
HTTP/1.1 201 Created
Content-Type: application/json; charset=utf-8

{"id": 101, "title": "demo", "body": "hello", "userId": 1}


https://jsonplaceholder.typicode.com:443/users/1?include=email#section
└─┬─┘ └──────────┬──────────────┘ └┬┘└──┬──┘ └──────┬───────┘ └──┬──┘
  协议              主机            端口   路径         查询参数       锚点
 scheme            host            port   path         query        fragment

# URL 组成部分

| 组成部分 | 示例 | 课堂解释 |
| --- | --- | --- |
| 协议 scheme | `https` | 浏览器与服务端通信的规则，http 明文传输，https 加密传输 |
| 主机 host | `jsonplaceholder.typicode.com` | 域名，需要通过 DNS 解析成 IP 才能访问 |
| 端口 port | `443` | 服务进程的“门牌号”，http 默认 80，https 默认 443 |
| 路径 path | `/users/1` | 服务器上资源的地址 |
| 查询参数 query | `?include=email` | 附加条件，用 `&` 连接多个 |
| 锚点 fragment | `#section` | 页面内定位，**不会发给服务器** |


# 常见端口说明

| 端口 | 服务 | 课堂场景 |
| --- | --- | --- |
| 80 | HTTP | 默认可省略不写 |
| 443 | HTTPS | 默认可省略不写 |
| 8080 | 开发常用 | 本地调试常用，如 `localhost:8080` |
| 3000 | 前端开发 | React/Vue 默认端口 |
| 5000 | Flask 默认 | Python 后端常用 |
| 8000 | Django 默认 | Python 后端常用 |
| 22 | SSH | 远程登录服务器 |

# HTTP 与 HTTPS 对比

| 维度 | HTTP | HTTPS |
| --- | --- | --- |
| 默认端口 | 80 | 443 |
| 传输内容 | 明文（可被抓包查看） | 加密（TLS/SSL） |
| 是否需要证书 | 否 | 需要 CA 证书 |
| 安全性 | 低，密码会被截获 | 高，密码不可读 |
| 浏览器标识 | 显示“不安全” | 显示锁头 |
| 课堂建议 | 仅本地调试可用 | 生产环境必须用 |

# 代理使用场景

| 场景 | 是否需要代理 |
| --- | --- |
| 公司内网访问外部 API | 需要，走公司网关 |
| 国内访问 jsonplaceholder 慢 | 可选，用代理加速 |
| 抓包调试移动端 | 需要，走 Charles/Fiddler 代理 |
| 课堂环境 | 若网络正常则不用，若访问超时再配置 |

# 浏览器开发者模式Network 常用功能

| 功能 | 位置 | 作用 | 课堂场景 |
| --- | --- | --- | --- |
| **Filter 过滤** | 面板顶部下拉框 | 只看 XHR/Fetch 请求，过滤掉图片/CSS/JS | 找出 AJAX 请求，排除干扰 |
| **Preserve Log** | 面板顶部勾选框 | 页面跳转后保留日志 | 调试登录跳转后请求丢失 |
| **Disable Cache** | 面板顶部勾选框 | 禁用缓存，每次都重新请求 | 排查“改了代码但没生效” |
| **Throttling 模拟弱网** | 面板下拉 → Online 切换 | 模拟 3G/4G 网络 | 测试接口在弱网下的表现 |
| **Replay 重发请求** | 右键请求 → Replay | 不刷新页面重发 | 修改 Header 后快速复测 |
| **Copy as cURL** | 右键请求 → Copy → Copy as cURL | 复制为命令行格式 | 把请求发给后端同事复现 |
| **Block request URL** | 右键 → Block request domain | 屏蔽某请求 | 模拟某接口失败的场景 |

# HTTP 请求方法说明

| 方法 | 典型用途 | 是否常带 Body | 课堂记忆方式 |
| --- | --- | --- | --- |
| GET | 查询资源 | 否 | 查 |
| POST | 创建资源 / 提交操作 | 是 | 增 / 提交 |
| PUT | 完整更新资源 | 是 | 整体改 |
| PATCH | 部分更新资源 | 是 | 局部改 |
| DELETE | 删除资源 | 否 | 删 |

# HTTP 状态码说明

| 状态码 | 含义 | 常见原因 | 课堂处理方式 |
| --- | --- | --- | --- |
| 200 | 请求成功 | 查询或操作成功 | 查看响应体 |
| 201 | 创建成功 | POST 创建资源成功 | 记录新资源 ID |
| 400 | 请求错误 | 参数格式错、缺字段 | 检查 Params / Body |
| 401 | 未认证 | 没登录、Token 缺失或失效 | 检查 Authorization |
| 403 | 无权限 | 当前身份不能访问 | 检查账号权限 |
| 404 | 资源不存在 | URL 写错或 ID 不存在 | 检查路径和资源 ID |
| 500 | 服务端错误 | 服务端代码或配置异常 | 记录错误并反馈服务端 |

# HTTP 状态码分类

| 分类 | 含义 | 课堂口诀 | 典型代表 |
| --- | --- | --- | --- |
| 1xx | 信息性 | “我知道了，继续” | `100 Continue` |
| 2xx | 成功 | “成了” | `200 OK`、`201 Created` |
| 3xx | 重定向 | “去别的地方找” | `301` 永久跳转、`302` 临时跳转、`304` 缓存命中 |
| 4xx | 客户端错误 | **“自己作怪”** | `400` / `401` / `403` / `404` / `429` |
| 5xx | 服务端错误 | **“服务端挂了”** | `500` / `502` / `503` |

# 常见 HTTP 状态码补充

| 状态码 | 含义 | 课堂场景 |
| --- | --- | --- |
| `204` | 成功但无内容返回 | DELETE 成功后常见 |
| `301` | 永久重定向 | 域名换地址了 |
| `302` | 临时重定向 | 登录后跳转回原页 |
| `304` | 命中缓存 | 浏览器缓存还在用 |
| `429` | 触发限流 | 调用太频繁，等会儿再试 |
| `502` | 网关错误 | 后端服务挂了 |
| `503` | 服务不可用 | 服务器维护中 |
| `504` | 网关超时 | 后端响应太慢 |

# 常见 HTTP Header 说明

| Header | 作用 | 示例 |
| --- | --- | --- |
| `Content-Type` | 告诉服务端请求体格式 | `application/json` |
| `Accept` | 告诉服务端希望返回的格式 | `application/json` |
| `Authorization` | 传递身份认证信息 | `Bearer <YOUR_API_KEY>` |
| `User-Agent` | 说明客户端类型 | 浏览器或工具自动生成 |
| `Host` | 指定服务端域名（HTTP/1.1 必需） | `jsonplaceholder.typicode.com` |
| `Referer` | 标识请求来源页 | 防盗链、统计分析 |
| `Cookie` | 携带之前服务端设置的 Cookie | 登录态保持 |
| `Origin` | 标识请求来源（跨域） | CORS 检查 |
| `X-Request-ID` | 请求追踪 ID | 排查问题必备 |
| `X-RateLimit-Remaining` | 剩余调用次数 | 限流提示 |


# 常见 Content-Type 类型说明

| Content-Type | 用途 | 课堂场景 |
| --- | --- | --- |
| `application/json` | JSON 数据 | 现代接口默认 |
| `application/x-www-form-urlencoded` | 表单提交 | 网页登录表单 |
| `multipart/form-data` | 上传文件 | 上传头像 |
| `text/html` | HTML 网页 | 浏览器渲染页面 |
| `text/plain` | 纯文本 | 简单文本响应 |
| `application/octet-stream` | 二进制流 | 下载文件 |

Cookie 与 Session（重要补充）：

为什么需要 Cookie？ HTTP 是"无状态"的，服务端默认记不住你上一次请求是谁。

Cookie 就是服务端发给客户端的"身份小本本"，下次请求带上它服务端就知道你是谁。

# Cookie 与 Session 对比

| 对比项 | Cookie | Session |
| --- | --- | --- |
| 存储位置 | 客户端（浏览器） | 服务端 |
| 容量 | 单条 ≤ 4KB | 不限（看服务端配置） |
| 生命周期 | 可设置过期时间 | 默认关闭浏览器即失效 |
| 安全性 | 较低（客户端可改） | 较高（服务端控制） |
| 工作流程 | 服务端 Set-Cookie → 客户端 Cookie | 服务端建 Session → 返回 Session ID 给 Cookie |

JSON 基本规则：

字符串必须使用双引号

对象使用 {}，数组使用 []

键和值之间使用冒号

多个字段之间使用逗号

最后一项后面不能多写逗号

# 单层JSON实例：
{
  "id": 1,
  "name": "小明",
  "email": "xm@example.com"
}

# 对应 Python dict
data = {"id": 1, "name": "小明", "email": "xm@example.com"}

# 取值
data["name"]        # -> "小明"
data.get("email")   # -> "xm@example.com"

# 实例2：嵌套对象
{
  "code": 0,
  "message": "success",
  "data": {
    "user": {
      "id": 1,
      "profile": {
        "name": "小明",
        "age": 18
      }
    }
  }
}
# 对应的 Python
# 多层取值
response["data"]["user"]["profile"]["name"]  # -> "小明"
response["data"]["user"]["profile"]["age"]   # -> 18

# 安全取值（推荐，防止 KeyError）
response.get("data", {}).get("user", {}).get("profile", {}).get("name")

# 实例3：数组与对象混合
{
  "data": {
    "users": [
      {"id": 1, "name": "小明"},
      {"id": 2, "name": "小红"}
    ],
    "total": 2
  }
}

# 对应的 Python
# 取列表
users = response["data"]["users"]  # -> [{...}, {...}]
users[0]["name"]                   # -> "小明"
users[1]["id"]                     # -> 2

# 遍历取所有 name
for user in users:
    print(user["name"])  # 小明 / 小红

# JSON 类型与 Python 类型对应关系

| JSON 类型 | Python 类型 | 示例 |
| --- | --- | --- |
| 对象 `{}` | `dict` | `{"name": "小明"}` |
| 数组 `[]` | `list` | `[1, 2, 3]` |
| 字符串 | `str` | `"hello"` |
| 数字 | `int` / `float` | `18` / `3.14` |
| 布尔 | `bool` | `true` → `True`（注意大小写！） |
| `null` | `None` | `null` → `None` |

import json

# JSON 字符串 -> Python 字典（Day10 API 响应解析会用到）
text = '{"name": "小明", "age": 18}'
data = json.loads(text)
print(data["name"])  # 小明

# Python 字典 -> JSON 字符串（Day10 发送请求体时会用到）
payload = {"title": "测试", "body": "hello"}
text = json.dumps(payload, ensure_ascii=False)
print(text)  # {"title": "测试", "body": "hello"}

重点：

json.loads() 把字符串变成 Python 对象（Day10 解析响应必用）

json.dumps() 把 Python 对象变成字符串（Day10 构造请求体必用）

ensure_ascii=False 让中文不被转义成 \uXXXX

HTTP 超时（重要补充）：
为什么必须设超时？ 如果服务端不响应，你的程序会一直等下去，永远卡死。
这是 Day08 AppConfig.api_timeout 的真正用途。

# HTTP 请求超时类型

| 超时类型 | 含义 | 典型值 |
| --- | --- | --- |
| `连接超时 Connect Timeout` | 与服务端建立连接的时间上限 | 5 秒 |
| `读取超时 Read Timeout` | 等待服务端返回数据的总时间 | 30 秒 |
| `总超时 Total Timeout` | 从发请求到拿到响应的总时间 | 30–60 秒 |

重点：
永远不要写 requests.get(url) 不带 timeout（Day10 会强调）

课堂推荐 timeout=10（10 秒）作为默认值

超时后可以结合 Day06 的 retry 装饰器自动重试

# 幂等性（RESTful 设计补充）：

# HTTP 方法幂等性说明

| 方法 | 幂等？ | 课堂解释 |
| --- | --- | --- |
| `GET` | ✅ | 多次请求结果一致 |
| `POST` | ❌ | 多次请求会创建多条数据 |
| `PUT` | ✅ | 多次更新结果一致 |
| `DELETE` | ✅ | 删一次和删十次结果一样（都已删除） |
| `PATCH` | ❌ | 部分更新的语义不保证幂等 |

重点：

幂等性 = 同一请求重复执行，结果不变

DELETE 幂等的典型例子：删一次返回 204，删第二次也返回 204（或 404）

设计 API 时，幂等的操作可以安全重试（Day06 retry 装饰器的前提

# get 请求实例

GET https://jsonplaceholder.typicode.com/posts?userId=1&_limit=3

Accept: application/json


# POST JSON 请求示例：

POST https://jsonplaceholder.typicode.com/posts
Content-Type: application/json

{
  "title": "Day09",
  "body": "网络基础入门",
  "userId": 1
}

# 工具中常见 Body 写法：

{
  "title": "Day09",
  "body": "学习 HTTP 和 RESTful API",
  "userId": 1
}

# RESTful API 的课堂理解：
资源 URL + HTTP 方法 + 状态码 + JSON 响应 = 一次规范的接口设计

# RESTful API URL 设计规范

| 规则 | 推荐写法 | 不推荐写法 | 原因 |
| --- | --- | --- | --- |
| 使用名词 | `/users` | `/getUsers` | 动作用方法表达 |
| 使用复数 | `/courses` | `/course` | 表示资源集合 |
| 小写字母 | `/course-groups` | `/courseGroups` | 风格统一 |
| 层级清晰 | `/courses/1/lessons` | `/courses/1/a/b/c` | 避免过深嵌套 |
| 带版本号 | `/api/v1/courses` | `/api/courses` | 方便后续升级 |

# CRUD 与 HTTP 方法对应

| 操作 | 方法 | URL 示例 | 说明 |
| --- | --- | --- | --- |
| 创建 | `POST` | `/api/v1/courses` | 新增课程 |
| 查询列表 | `GET` | `/api/v1/courses` | 获取课程列表 |
| 查询详情 | `GET` | `/api/v1/courses/1` | 获取单个课程 |
| 完整更新 | `PUT` | `/api/v1/courses/1` | 替换课程完整信息 |
| 部分更新 | `PATCH` | `/api/v1/courses/1` | 修改部分字段 |
| 删除 | `DELETE` | `/api/v1/courses/1` | 删除课程 |

# API 统一响应格式

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| `code` | 业务状态码 | `0` |
| `message` | 提示信息 | `success` |
| `data` | 业务数据 | 课程列表或课程详情 |
| `request_id` | 请求追踪编号 | `req_001` |

# 成功响应实例
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "Python API 开发入门"
  },
  "request_id": "req_20260719_001"
}

# 错误响应统一格式（结合 Day08 AppError 设计）

{
  "code": 4001,
  "message": "课程不存在",
  "data": null,
  "request_id": "req_20260719_002",
  "errors": [
    {
      "field": "id",
      "message": "课程 ID 999 不存在"
    }
  ]
}

# 业务状态码设计规范

| 业务码段 | 含义 | 示例 |
| --- | --- | --- |
| `0` | 成功 | `0` |
| `1xxx` | 通用错误（参数/校验） | `1001` 参数缺失 |
| `2xxx` | 用户/认证类错误 | `2001` 未登录、`2003` 无权限 |
| `3xxx` | 业务错误（课程/订单） | `3001` 课程不存在 |
| `4xxx` | 第三方依赖错误 | `4001` 短信发送失败 |
| `5xxx` | 系统错误 | `5000` 服务异常 |

# HTTP 状态码与业务码对应关系

| 情况 | HTTP 状态码 | 业务码 | 课堂解释 |
| --- | --- | --- | --- |
| 成功 | `200` | `0` | 接口通了，业务也成功 |
| 参数错 | `400` | `1001` | 接口通了，但参数有问题 |
| 未登录 | `401` | `2001` | 接口通了，但没身份 |
| 资源不存在 | `404` | `3001` | 接口通了，但资源没了 |
| 服务异常 | `500` | `5000` | 接口本身出问题了 |

重点：

HTTP 状态码判断"通信层"是否成功

业务码判断"业务逻辑"是否成功

Day10 写代码时要同时检查：先看 HTTP 状态码 200，再看业务码 0

# API 查询参数设计（分页/排序/过滤/搜索）

| 参数类型 | 参数名 | 示例 | 说明 |
| --- | --- | --- | --- |
| 分页 | `page`、`size` | `?page=1&size=10` | 第 1 页，每页 10 条 |
| 排序 | `sort` | `?sort=-created_at` | 按创建时间倒序（`-` 表示降序） |
| 过滤 | `status`、`type` | `?status=published` | 只看已发布的 |
| 搜索 | `q` 或 `keyword` | `?q=Python` | 全文搜索关键词 |
| 字段过滤 | `fields` | `?fields=id,name` | 只返回 id 和 name 字段 |

# 完整查询示例
GET /api/v1/courses?page=1&size=10&
sort=-created_at&status=published&q=Python&fields=id,name,price

# RESTful API 批量操作设计

| 操作 | 方法 | URL | 请求体 | 说明 |
| --- | --- | --- | --- | --- |
| 批量删除 | `DELETE` | `/api/v1/courses?ids=1,2,3` | 无 | 通过 query 参数传 ID 列表 |
| 批量删除 | `POST` | `/api/v1/courses/batch-delete` | `{"ids": [1, 2, 3]}` | 通过 body 传 ID 列表 |
| 批量创建 | `POST` | `/api/v1/courses/batch` | `{"items": [...]}` | 一次创建多条 |
| 批量更新 | `PATCH` | `/api/v1/courses/batch` | `{"items": [{"id":1, ...}]}` | 一次更新多条 |

# RESTful API URL 层级设计

| 层级 | 示例 | 是否推荐 |
| --- | --- | --- |
| 1 层 | `/courses` | ✅ 推荐 |
| 2 层 | `/courses/1/lessons` | ✅ 推荐 |
| 3 层 | `/courses/1/lessons/2/files` | ⚠ 可以接受 |
| 4 层+ | `/courses/1/lessons/2/files/3/versions` | ❌ 不推荐，应拆分 |

规则： 嵌套不超过 3 层，超过的改用子资源独立 URL，如：
❌ /courses/1/lessons/2/files/3/versions
✅ /files/3/versions（用 file_id=3 查询参数表达归属）

# API 版本管理策略

| 版本策略 | 示例 | 优点 | 缺点 |
| --- | --- | --- | --- |
| URL 路径版本 | `/api/v1/courses` | 直观，简单 | 升级版本需要改 URL |
| Header 版本 | `Accept: application/vnd.api+json;version=1` | URL 干净 | 不直观，调试麻烦 |
| 查询参数版本 | `/api/courses?version=1` | 灵活 | 容易忘传 |

# 限流响应头1
HTTP/1.1 200 OK
X-RateLimit-Limit: 60              # 每分钟最大请求数
X-RateLimit-Remaining: 58          # 剩余可用次数
X-RateLimit-Reset: 1721376000     # 重置时间戳
Retry-After: 60                    # 触发 429 后建议等待秒数

# 触发限流时的响应
HTTP/1.1 429 Too Many Requests
Content-Type: application/json
Retry-After: 60

{
  "code": 4001,
  "message": "请求过于频繁，请稍后再试",
  "data": null
}

重点：

公共 API 通常有限流（如 GitHub 60 次/小时未认证）

Day10 调用 API 时遇到 429 应该 sleep 后重试，而不是继续轰炸

结合 Day06 retry 装饰器 + 指数退避策略

# 正确的课程管理 API 设计示例

| 功能 | 方法 | URL | 参数说明 | 响应说明 |
| --- | --- | --- | --- | --- |
| 获取课程列表 | `GET` | `/api/v1/courses` | `page`、`size` | 返回课程列表 |
| 获取课程详情 | `GET` | `/api/v1/courses/{id}` | 路径参数 `id` | 返回单个课程 |
| 创建课程 | `POST` | `/api/v1/courses` | JSON 请求体 | 返回新课程 ID |
| 完整更新课程 | `PUT` | `/api/v1/courses/{id}` | 完整课程 JSON | 返回更新结果 |
| 修改课程状态 | `PATCH` | `/api/v1/courses/{id}` | `status` | 返回更新结果 |
| 删除课程 | `DELETE` | `/api/v1/courses/{id}` | 路径参数 `id` | 返回删除结果 |
| 获取课程章节 | `GET` | `/api/v1/courses/{id}/lessons` | 课程 ID | 返回章节列表 |

# 错误设计对比：

/getAllCourses              # 错误：URL 使用动词
/createCourse               # 错误：动作没有交给 POST
/api/v1/course              # 不推荐：集合资源建议用复数
/api/v1/CourseGroups        # 错误：大小写风格不统一
/api/v1/courses/1/2/3/a/b   # 错误：嵌套过深

# 成功响应实例：
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "name": "Python API 开发入门"
  }
}

# 第五部分：
