1.项目文件必须在Pythonproject文件下
2.常用的虚拟环境的指令
# 查看 conda 是否可用
conda --version

# 查看所有环境
conda env list

# 创建一个 Python 3.10 的环境，名字叫 ai_dev
conda create -n ai_dev python=3.10

# 激活环境
conda activate ai_dev

# 查看当前环境的 Python
python --version

# 安装基础库
conda install numpy pandas requests

# 退出当前环境
conda deactivate

# 删除当前环境
conda remove -n ai_dev --all

# 环境验证脚本
# test.py
import sys
import requests

print(f"Python 版本: {sys.version}")
print(f"Python 路径: {sys.executable}")

response = requests.get("https://www.baidu.com", timeout=5)
print(f"请求状态码: {response.status_code}")

venv（Python 内置）：
优点：无需额外安装，跟着 Python 走
局限：不能管理 Python 解释器版本本身
conda 环境：
优点：可以指定不同 Python 版本，能装非 Python 依赖
局限：体积偏大，启动慢一些
requirements.txt：
项目依赖的"清单"
pip freeze > requirements.txt 导出
pip install -r requirements.txt 一键还原
