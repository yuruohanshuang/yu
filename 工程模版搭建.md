
# 快捷创建根目录指令，不过需要提前下载git git：让指令之间可以通用
# 进入工作根目录后执行（Windows / macOS / Linux 通用）
mkdir ai_template && cd ai_template

# 源码与子模块
mkdir -p src/config src/utils src/api src/services
mkdir -p tests config logs data/raw data/processed docs scripts

# 创建 __init__.py 与占位文件（Linux / macOS 用 touch；Windows 见下方说明）
touch src/__init__.py src/config/__init__.py src/utils/__init__.py
touch src/api/__init__.py src/services/__init__.py tests/__init__.py
touch logs/.gitkeep data/.gitkeep
touch .gitignore .env.example requirements.txt requirements-dev.txt
touch pyproject.toml README.md


black:一款"固执己见"的格式化工具，几乎不提供配置项
自动处理缩进、空格、引号、换行
sort:专门排序 import 语句
按"标准库 → 第三方库 → 本地模块"分组并空行分隔
与 Black 配合时必须用 profile = "black"，避免互相打架
