目录与文件操作三件套：
pwd 显示当前位置
cd <path> 转换位置
ls -la  详细列出 查看隐藏文件
mkdir -p <path> 递归建目录  mkdir -p logs/2026
touch <file> 创建空文件 建占位 .py
cp / mv / rm  复制 / 移动 / 删除 rm -rf 慎用
cat / head / tail 看文件内容 tail -f app.log 追日志
python --version  查看版本
python script.py 运行脚本
python -m pkg.mod 以模块方式运行
pip list 已装包清单
pip freeze > requirements.txt  导出依赖
模式                      作用                            如何进入
Normal（普通模式）  移动光标、复制粘贴、删除          默认进入，按 Esc 回到此模式
Insert（插入模式）       输入文字                           按 i 进入
Visual（可视模式）       选中文本块                         按 v 进入
输入代码...
按 Esc              # 回到普通模式
:w                  # 保存（不退出）
:q                  # 退出（不保存时）
:wq                 # 保存并退出
:q!                 # 强制退出（放弃修改）
