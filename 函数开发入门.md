写法                  含义              版本要求
x: int          参数 x 是 int          所有版本
-> str          返回值是 str            所有版本
list[int]       元素为 int 的列表         3.9+
dict[str, int]  键 str、值 int 的字典     3.9+
int | None      可为 int 或 None         3.10+
Optional[int]   等价于 int | None      需 typing

类型                  写法              调用方式            特点
位置参数             def f(a, b)         f(1, 2)         顺序固定
关键字参数           def f(a, b)         f(a=1, b=2)     顺序可换
默认参数            def f(a, b=10)      f(1)            不传用默认
强制关键字           def f(a, *, b)      f(1, b=2)       b 必须用名字传

形式          类型          收集
*args       tuple       多余的位置参数
**kwargs    dict        多余的关键字参数

完整参数顺序
位置参数 → 默认参数 → *args → 强制关键字参数 → **kwargs

场景          位置                  写法                      作用
赋值解包      等号左边      a, *rest, b = lst             把序列拆给多个变量
调用解包    函数调用括号里     f(*lst) / f(**d)\           把容器拆成多个参数传入
重点：
* 函数定义里的 *args 是收集，调用时的 *lst 是解包，方向相反
* 用于 list/tuple（拆成位置参数），** 用于 dict（拆成关键字参数）
* 赋值解包中的 *rest 收集成 list；调用解包中的 *nums 是"展开"
想做的事                          该用
传任意多个数字                   *args
传任意多个配置项（带名字）         **kwargs

修改外层变量的两个关键字：
关键字                                 作用
global x                    在函数内修改模块级变量
nonlocal x          在内层函数内修改外层函数（非全局）的变量

示例演示：闭包计数器
def make_counter(start: int = 0):
    """创建独立的计数器闭包."""
    count = start

    def step(delta: int = 1) -> int:
        nonlocal count
        count += delta
        return count

    return step


c1 = make_counter()
c2 = make_counter(100)
print(c1())     # 1
print(c1())     # 2
print(c2(5))    # 105
print(c2(5))    # 110   c1 与 c2 互不影响

* nonlocal count 这一行是关键，没有它会被当作局部变量重新创建
* c1 与 c2 各自携带一份独立的 count，这就是闭包的"环境捕获"
* 闭包常用于"工厂函数"——后面 OOP 章会反过来对照讲

对比/规则说明
关键字           作用域跨度
global        函数内 → 模块顶层
nonlocal    内层函数 → 外层（非全局）函数

高阶函数：接收函数作为参数的函数
nums = [1, 2, 3, 4, 5]

# map：对每个元素应用函数
squared = list(map(lambda x: x ** 2, nums))
print(squared)   # [1, 4, 9, 16, 25]

# 等价于列表推导式
squared = [x ** 2 for x in nums]

# filter：按条件过滤
evens = list(filter(lambda x: x % 2 == 0, nums))
print(evens)     # [2, 4]

# 等价于列表推导式
evens = [x for x in nums if x % 2 == 0]

# sorted：按自定义规则排序
students = [("张三", 85), ("李四", 92), ("王五", 78)]
by_score = sorted(students, key=lambda s: s[1], reverse=True)
print(by_score)   # [('李四', 92), ('张三', 85), ('王五', 78)]