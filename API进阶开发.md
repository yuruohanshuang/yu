# APIClient 职责判断

| 功能 | 是否放进 APIClient | 理由 |
| --- | --- | --- |
| `base_url` 拼接 | 是 | 统一处理请求地址，避免重复拼接 |
| `Authorization Header` | 是 | 统一管理认证请求头，避免每次请求重复设置 |
| 打印页面菜单 | 否 | 属于页面展示层职责，与 HTTP 请求无关 |
| 请求失败重试 | 是 | 统一处理请求失败后的重试逻辑 |
| 保存数据库 | 否 | 属于数据持久化层职责，不应由 APIClient 负责 |
| 解析 JSON 响应 | 是 | 统一处理响应数据，简化业务代码 |